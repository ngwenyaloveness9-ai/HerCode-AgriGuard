import FarmTwin from '../components/FarmTwin';
export default function DigitalTwinPage({data}){return <section className="page"><div className="page-title"><div><span className="eyebrow">SPATIAL OPERATIONS</span><h1>Farm Digital Twin</h1><p>A simple visual model of the physical AgriGuard prototype and its live state.</p></div><div className="status-pill good">● SYNCHRONIZED</div></div><div className="twin-stage"><FarmTwin data={data}/><div className="scanline"></div></div><div className="three-grid">{data.zones.map(z=><div className="glass-card" key={z.id}><span className="eyebrow">ZONE {z.id}</span><h3>{z.crop}</h3><div className="big-number">{z.moisture}<small>% moisture</small></div><div className={`health ${z.moisture<30?'danger':'good'}`}>{z.moisture<30?'STRESS DETECTED':'HEALTHY'}</div></div>)}</div></section>}