import ZoneCard from '../components/ZoneCard'; export default function ZonesPage({data}){return <section><div className="pageHeading"><span className="eyebrow">LIVE SOIL SENSORS</span><h2>Crop Zones</h2><p>Raw 12-bit ADC values come directly from GPIO34, GPIO35 and GPIO32. Percentages are calculated by the backend from configurable dry/wet calibration values.</p></div><div className="zoneGrid">{data.zones.map(z=><ZoneCard key={z.id} zone={z}/>)}</div><div className="panel infoPanel"><h3>Physical layout</h3><p>Zone A: Macadamia • GPIO34. Zone B: Macadamia • GPIO35. Zone C: Citrus • GPIO32. The Arduino publishes moistureRaw for each zone every 3 seconds.</p></div></section>}
