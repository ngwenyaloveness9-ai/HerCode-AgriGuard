import {Droplets,Power,Sprout} from 'lucide-react';
export default function FarmTwin({data}){return <div className="panel twin"><div className="panelTitle"><div><span className="eyebrow">LIVE FARM VIEW</span><h3>Physical sensor status</h3></div><Droplets/></div><div className="beds">{data.zones.map(z=><div className={`bed ${z.moisture<30?'dry':''}`} key={z.id}><div className="cropRows">{[1,2,3,4,5,6,7,8].map(n=><Sprout key={n}/>)}</div><div className="bedInfo"><b>Zone {z.id}</b><span>{z.crop}</span><strong>ADC {z.raw} • {z.moisture}% est.</strong></div></div>)}</div><div className="waterline"><Droplets/> Reservoir {data.reservoir}% <span>→</span><Power/> Pump {data.pumpOn?'ON':'OFF'} <span>→</span> Relay GPIO27</div></div>}
