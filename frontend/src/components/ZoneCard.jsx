import {Droplets,Sprout} from 'lucide-react'; import {moistureStatus} from '../utils/system';
export default function ZoneCard({zone}){const s=moistureStatus(zone.moisture);return <article className={`zoneCard panel ${s==='DRY'?'danger':''}`}><div className="zoneTop"><span>ZONE {zone.id}</span><b className={s.toLowerCase()}>{s}</b></div><div className="zoneCrop"><Sprout/><div><h3>{zone.crop}</h3><p>{zone.name}</p></div></div><div className="zoneReading"><Droplets/><strong>{zone.moisture}%</strong><span>Estimated calibrated moisture</span></div><div className="raw">LIVE ADC raw value <b>{zone.raw}</b></div></article>}
